{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm install --prefix apps/web && cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
